#pragma once

enum mainMenuItem {
	TEXT = 1,
	SYMBOLS,
	FIND_WORDS,
	UNIT_TESTS,
	ABOUT,
	QUIT
};

enum inputTypeMenuItem {
	CONSOLE_INPUT = 1,
	FILE_INPUT
};

enum rewriteMenuItem {
	REWRITE = 1,
	BACK
};

enum savingMenuItem {
	SAVE_TEXT = 1,
	SAVE_SYMBOLS,
	SAVE_RESULT,
	SKIP
};